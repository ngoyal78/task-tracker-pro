# requirements.txt

Django>=4.2,<5.0
gunicorn
psycopg2-binary  # Needed if using PostgreSQL on Railway
whitenoise  # For serving static files in production
dj-database-url  # To parse DATABASE_URL from environment variables
python-dotenv  # For local environment variables support
